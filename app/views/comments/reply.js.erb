$('li#new-comment').html("<%=
  j render partial: 'comments/form',
  locals: { commentable: @commentable, comment: @comment }
%>");
$('html, body').animate({
  scrollTop: $('textarea#comment_content').offset().top
}, 1000);
$('textarea#comment_content').focus();
