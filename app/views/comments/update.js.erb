$("li#comment-<%= @comment.id %>").html("<%=
  j render partial: 'comments/show', locals: { app: @app, comment: @comment }
%>");

$('li#comment-<%= @comment.id %> .markdown').each(function () {
  markdown($(this).html(), $(this));
});
