<p id="notice"><%= notice %></p>

<h1>Listing User Options</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Front cover</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_options.each do |user_option| %>
      <tr>
        <td><%= user_option.user_id %></td>
        <td><%= user_option.front_cover %></td>
        <td><%= link_to 'Show', user_option %></td>
        <td><%= link_to 'Edit', edit_user_option_path(user_option) %></td>
        <td><%= link_to 'Destroy', user_option, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User option', new_user_option_path %>
