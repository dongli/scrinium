<div class='publisher-form'>
  <%= simple_form_for @publisher do |f| %>
    <%= f.input :name %>
    <%= f.input :abbreviation %>
    <%= f.input :short_name %>
    <% if policy(Publisher).qualify? %>
      <%= f.hidden_field :status, value: 'qualified' %>
    <% end %>
    <div class='center'>
      <%= f.button :submit, t('action.submit'), class: 'button' %>
    </div>
  <% end %>
</div>
