<%= fa_icon('plus', style: 'margin-right: 10px;') %>
<%= link_to activity.owner.name, activity.owner if activity.owner %>
发表文章
<% if activity.trackable %>
  《<%= link_to activity.trackable.title, activity.trackable %>》
<% else %>
  ，不过已经被删除了。
<% end %>
