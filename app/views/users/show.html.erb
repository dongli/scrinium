<% content_for :title, t('user.worktable') %>

<% content_for :left_column do %>
  <%= render 'users/profile_left' %>
<% end %>

<% content_for :center_column do %>
  <%=
    render 'layouts/navtab',
    tabs: {
      columns: [
        t('user.columns'),
        render('users/articles', user: @user)
      ],
      publications: [
        t('user.publications'),
        render('users/publications', user: @user)
      ],
      collections: [
        t('collection'),
        render('users/collections', user: @user)
      ],
      resources: [
        t('activerecord.models.resource'),
        render('resource_board/show', folderable: @user, user: @user)
      ]
    }
  %>
<% end %>
