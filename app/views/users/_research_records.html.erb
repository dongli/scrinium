<dl>
  <dt><%= t('user.latest_research_records') %></dt>
  <dd>
    <%=
      render partial: 'research_records/table_list',
      locals: {
        research_records: user.research_records,
        show_author: false
      }
    %>
    <% if is_same_user? user %>
      <%= link_to t('action.add'), new_research_record_path, class: 'button right' %>
    <% end %>
  </dd>
</dl>
