<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= content_for?(:title) ? yield(:title) + ' - ' + t('global.scrinium') : t('global.scrinium') %></title>
  <%= yield(:head) if content_for? :head %>
  <!-- Set ROOT_PATH for querying in JavaScript. -->
  <script type="text/javascript" charset="utf-8">
    var ROOT_PATH = '<%= main_app.root_url %>';
    I18n = {}
    I18n.defaultLocale = '<%= I18n.default_locale %>';
    I18n.locale = '<%= I18n.locale %>';
  </script>
  <script type="text/x-mathjax-config">
    MathJax.Hub.Config({
      TeX: { equationNumbers: { autoNumber: "all" } }
    });
  </script>
  <%= mathjax_tag %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <script src="message-bus.js" type="text/javascript"></script>
  <%= csrf_meta_tags %>
</head>
<body>

  <header>
    <%= render 'layouts/navbar' %>
  </header>

  <div id="main-container" class="container">
    <%= render 'layouts/message' %>
    <%= render_breadcrumbs %>
    <%= render 'layouts/left_side' %>
    <%= render 'layouts/main_block' %>
  </div>

  <footer>
    <% if current_user and
          current_user.organization and
          current_user.organization.short_name != 'void' %>
      Copyright &copy 2015 <%= current_user.organization.name %> / <%= t('global.shustone') %>
    <% else %>
      Copyright &copy 2015 <%= t('global.shustone') %>
    <% end %>
  </footer>
</body>
</html>
