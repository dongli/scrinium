<div class='center'>
  <%= simple_form_for [group, node] do |f| %>
    <%= f.input :name, required: false %>
    <%= f.input :position %>
    <%= f.submit t('action.submit'), class: 'btn btn-success' %>
  <% end %>
</div>
