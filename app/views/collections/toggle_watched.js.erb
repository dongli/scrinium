if (<%= @collection.watched %>) {
  $('#collection-<%= @collection.id %>').find('.fa').attr('class', 'fa fa-eye');
} else {
  $('#collection-<%= @collection.id %>').find('.fa').attr('class', 'fa fa-eye-slash');
}
