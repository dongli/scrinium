<%= simple_form_for @journal do |f| %>
  <%= f.input :name, label: t('journal.name') %>
  <%= f.input :abbreviation, label: t('journal.abbreviation') %>
  <%= f.button :submit, t('action.submit'), class: 'button float-right' %>
<% end %>
