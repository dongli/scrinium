<% content_for :title, @group.name %>

<% content_for :left_side do %>
  <%= render 'profile_left' %>
<% end %>

<% content_for :main_block do %>
  <dl>
    <dt><%= t('group.description') %></dt>
    <dd><%= markdown @group.description %></dd>
  </dl>
  <dl>
    <dt><%= t('group.research_records') %></dt>
    <dd>
      <%=
        render partial: 'research_records/table_list',
        locals: { research_records: @group.research_records }
      %>
    </dd>
  </dl>
<% end %>
