<% content_for :title, t('action.add')+t('activerecord.models.group') %>

<% content_for :center_column do %>
  <div class='panel panel-default box-shadow'>
    <div class='panel-body'>
      <%= render 'form' %>
    </div>
  </div>
<% end %>
